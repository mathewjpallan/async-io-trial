# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
GET /play/asyncapi/:delaytime                   controllers.TestController.testAsync(delaytime: String)
GET /play/syncapi/:delaytime                   controllers.TestController.testSync(delaytime: String)